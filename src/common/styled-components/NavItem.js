import styled from 'styled-components';

const NavItem = styled.li`
    width: 100%;
    height: 3em;
    display: flex;
    align-items: center;
    position: relative;
    font-size: 20px;
    font-weight: 700;
    color: #D1D5DB;
`;

export default NavItem;
